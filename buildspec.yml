version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd backend
      - npm install
  build:
    commands:
      - echo "Backend build complete"
artifacts:
  files:
    - '**/*'
  base-directory: backend

